#ifndef APPLY_HPP
#define APPLY_HPP

// v8/node
#include "include_v8.hpp"

namespace node_osmium {

    v8::Handle<v8::Value> apply(const v8::Arguments& args);

} // namespace node_osmium

#endif // APPLY_HPP
